extends layout

append head
  title Remote Lab - Constellation

block content
  .row: .col-xs-12
    h2 Remote Lab #[small#project]
  .row: .col-xs-12
    p
      | You can keep this page open and your editor &ldquo;collaborating&rdquo; for an entire lab session.
      | If you close this page or the editor is disconnected, restart the collaboration.
  
  .row: .col-xs-12: #error.alert.alert-danger
  
  .row: .col-xs-12
    h3#scratch-title Shared scratchpad #[small]
    textarea#scratch.form-control.text-editor(rows=8)
  
  .row: .col-xs-12
    h3 Editor files
    #files
  
  script.
    var shareURL = !{JSON.stringify(shareURL)};
    var collabid = !{JSON.stringify(collabid)};
  script(src='/public/sharedb.js')
  script(src='/static/lab.js')
  script(src='/static/connected.js')
