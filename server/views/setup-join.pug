extends layout

append head
  title Setup - Constellation
  script(src='/static/join.js')

block content
  .row: .col-xs-12
    h2 Setup
    h3 Connection test
  
  .row: .col-xs-10.col-sm-8.col-md-6.col-lg-4
    form#join
      .form-group
        h3 Are you ready?
      .form-group
        input#partner(type='hidden')
        button.btn.btn-danger.btn-lg Go!
    h3#status.text-primary
  
  script.
    rendezvous('', $('#join'), $('#partner'), $('#status'));
